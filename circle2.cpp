#include <iostream>
#include <cmath>

using namespace std;

int main() {
  cout << "Enter a radius: ";
  double radius;
  cin >> radius;
  const double pi = 3.14;
double pow(double base, double exponent);
double area = pi * pow(radius, 2);
  cout << area;
  return 0;
}
